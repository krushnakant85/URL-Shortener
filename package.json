{
  "name": "url-shortener-aws",
  "version": "0.1.0",
  "bin": {
    "url-shortener-aws": "bin/url-shortener-aws.js"
  },
  "scripts": {
    "build": "tsc && jest",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "destroy": "cdk destroy",
    "clean": "rm -rf cdk.out && rm -rf node_modules -r && npm install"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1007.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "aws-cdk-lib": "2.186.0",
    "constructs": "^10.0.0"
  }
}
